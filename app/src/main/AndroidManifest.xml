<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.touchsoul.aguaegas" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!-- Configuração da versão do Gooogle Play -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Configuração para usar chave do maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key" />

        <activity android:name=".View.Login" ></activity>
        <activity android:name=".View.Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.SelectUser" ></activity>
        <activity android:name=".View.ChooseService" ></activity>
        <activity android:name=".View.MapWaterCustomer"></activity>
        <activity android:name=".View.WaterProviders"></activity>
        <activity android:name=".View.ProviderMenu"></activity>
        <activity android:name=".View.OrdersList"></activity>
        <activity android:name=".View.ProviderSettings"></activity>
        <activity android:name=".View.MapGasCustomer"></activity>
    </application>

</manifest>